import 'package:flutter/material.dart';

class ManageRentalsScreen extends StatelessWidget {
  const ManageRentalsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    // ...implementasi UI kelola rental...
    return Scaffold(
      appBar: AppBar(title: const Text('Kelola Rental')),
      body: Center(child: Text('Kelola Rental')),
    );
  }
}
